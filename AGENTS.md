# AGENTS.md · 喜茶华南战区数据看板

面向在本目录工作的 AI 代理与协作者。**动手前先读完本文**，尤其是下面「已知特例」——那些是踩过坑之后刻意写成这样的，不要当成 bug 去"修好"。

---

## 1. 项目是什么

纯前端单文件看板，数据全内联，双击 HTML 即可离线打开。

```
Excel（评分数据） --build_data.py--> data.json --generate_html.py--> dashboard.html
```

三个视图：**区域周报总览** / **门店全景排名** / **督导视图**。
注意：这里的"周"指周期。数据文件夹现在按「8月评分数据 / 9月评分数据」命名（历史名字「双周基础数据MMDD-MMDD」也仍然认），周期长短由文件夹/文件名里的日期决定，见 §3。

### 工作区约束

- 路径含中文，PowerShell 下写文件请用 `Out-File -Encoding utf8` / `Set-Content -Encoding UTF8`，不要用默认编码。
- 跑 Python 建议加 `-X utf8`，否则中文 print 可能乱码。
- git 的写操作（add/commit/push）需要沙箱外权限，`.git` 目录对代理是只读的。
- `openpyxl` 读这些表会刷 `UserWarning: Unknown extension` / `Conditional Formatting extension`，无害，可 `2>$null` 屏蔽。

---

## 2. 常用命令

```bash
python -X utf8 build_data.py                                  # 全量重建所有期 -> data.json（慢）
python -X utf8 build_data.py --folder 9月评分数据             # 只重建这一期，写入 _data_cache/
python -X utf8 build_data.py --merge                          # 只合并现有缓存 -> data.json，不解析 Excel
python -X utf8 generate_html.py                               # data.json -> dashboard.html
python -X utf8 server.py [--port 8080] [--no-open]            # 本地实时服务（默认 8000）
```

- 期文件夹列表由 `build_data.py` 的 `list_period_folders()` 统一识别（规则见 §3）。**新增一期数据只要把文件夹丢进本目录，不用改代码**；`server.py` 直接 import 这个函数，所以两边规则永远一致。
- `server.py` 每 **2 秒**轮询：新出现的期文件夹会自动构建并加进「数据周期」下拉；已有期的 `(文件名, mtime_ns, size)` 签名变了才重建**那一期**。`build_data.py` / `generate_html.py` 自身变了会**全量**重建并 `importlib.reload` 识别规则。页面通过版本号长轮询自动刷新，无需手动刷新。
- 双击 `启动看板.bat` 即可：它用 `--no-open` 起服务，3 秒后再打开 `http://127.0.0.1:8000`。
- 改了同一期文件夹里的 Excel 之后，`--merge` **不会**重新解析，必须 `--folder <期>` 或全量构建。

---

## 3. 数据源

```
8月评分数据/                            # 期文件夹，一期一个（当前：8月评分数据 / 9月评分数据）
├── 评分数据/                           # ★ 唯一的数据来源；这一层可选，没有就直接平铺在期文件夹下
│   ├── 全国评分数据8月-美团.xlsx
│   ├── 全国评分数据8月-闪购.xlsx
│   └── 有公式/                         # 主流程只扫一层、不进这里；美团明细补数见 3.2
└── 基础数据/<期>.xlsx                  # 可选，只用来补 门店ID / 门店类型（当前工作区没有）
```

**期文件夹识别**（`_is_period_folder()`；满足任一条即算一期）：

1. 名字匹配 `^双周基础数据(\d{2})(\d{2})-(\d{2})(\d{2})$`，如 `双周基础数据0824-0830`；
2. 名字含「评分数据」或「基础数据」，如 `9月评分数据`；
3. 目录里直接放着文件名含「美团」/「闪购」的 `.xlsx`。

**周期区间**（`_period_range()`，优先级从高到低）：文件夹名里的 `MMDD-MMDD` → 目录内数据文件名里的 `M.D-M.D`（`9.1-9.13评分数据-美团.xlsx` → 2026-09-01 ~ 09-13）→ 文件夹名里的「N月」按整月。名字里写了 `YYYY年` 就用年份，否则用 `PERIOD_YEAR`。
**上期区间**（`_period_str()`）：优先取**上一期文件夹**的区间；没有上一期时，整月按上一自然月、非整月按同长度前移。
**最终标签**以（有公式）明细的「日期」列为准（3.2），比文件夹名更准——所以 8 月这一期显示 `2026-08-01 ~ 2026-08-31`、上期 `2026-07-01 ~ 2026-07-31`。

- 文件名含「美团」/「闪购」即被识别；`~$` 开头的 Excel 锁文件自动跳过。
- 两种目录布局都兼容：`<期>/评分数据/*.xlsx` 与 `<期>/*.xlsx`（「有公式」同理：`评分数据/有公式/` 或 `有公式/`）。
- **有公式文件**：`_rating_files()` 只扫一层、且跳过名字含「有公式」的文件，由 `_formula_file()` 单独定位，别把它并进主扫描。
- 基础数据底表读 `门店基础数据底表` sheet（门店名第 5 列、类型第 4 列、ID 第 6 列，从第 4 行起）。
- **`PERIOD_YEAR = 2026` 硬编码在 `build_data.py`**，跨年要改。
- 门店一律按**门店名字符串**匹配；名字对不上就当两家店。

### 取数口径（列号均为 0 基，与 Excel 字母对应已注明）

| Sheet | 键列 | 起始行 | 说明 |
| --- | --- | --- | --- |
| `华南门店维度` | 门店名 = 5（F） | 6 | 美团评分 6/7、二级指标 9–14；**第 15/16 列各期含义不同**，由 `_store_spec()` 按表头判断（8 月 = 本期/上期复购率，9 月起 = 本期/上期消息回复率） |
| `华南汇总` | 城市名 = 3（D） | 5 | **指标列比门店维度左移 1**（美团评分 = 5/6）；`战区` 为战区行，`影响分数` 行跳过 |
| `督导维度`（美团 8 月）/ `华南督导`（9 月，两个渠道同名）/ `督导`（闪购 8 月） | 督导名 = 1（B） | 3 | 本期/上期/变化**三元组**，起始列 2、5、8、11、14、17、20；`_read_rating_sups()` 依次尝试这三个 sheet 名 |

### 指标清单（18 项，美团 9 + 闪购 9）

- 美团：`mt_score` 美团评分、`mt_repeat` 复购率、`mt_goods_sat` 商品满意度、`mt_pack_sat` 包装满意度、`mt_repeat_score` 复购率指标得分、`mt_food_safe` 食品安全负反馈率、`mt_reply_score` 消息回复率得分、`mt_reply` 消息回复率（百分比）、`mt_service_fb` 服务负反馈率
- 闪购：`sg_score` 闪购评分、`sg_cancel` 商责取消率、`sg_taste_sat` 口味满意度、`sg_pack_sat` 包装满意度、`sg_repeat_score` 复购率指标得分、`sg_food_safe` 食品安全负反馈率、`sg_reply_score` 消息回复率**得分**、`sg_reply` 消息回复率（百分比）、`sg_service_fb` 服务负反馈率
- **美团门店维度不一定给「消息回复率」百分比列**：8 月只有「消息回复率得分（10%）」，`mt_reply` 要从（有公式）明细补；9 月起第 15/16 列直接给了「本期/上期消息回复率」，就不用补了。反过来 9 月起门店维度**没有**复购率，`mt_repeat` 改由明细补。两边都由 `_read_mt_detail()` 按「缺什么补什么」处理，见 3.2。
- 前端指标下拉顺序（`generate_html.py` 的 `PLATFORMS`）：美团 = 评分 → 二级指标 → 消息回复率得分 → 消息回复率 → 服务负反馈率；闪购同序，末尾追加商责取消率。**两个渠道的「消息回复率」都必须排在「商责取消率」前面。**

指标定义分散在三处，**改口径要同时改**：
`build_data.py` 的 `STORE_COLS` / `SUMMARY_COLS` / `SUP_COLS`，`generate_html.py` 的 `METRICS`（名称/单位/方向）与 `METRIC_HELP`（问号提示文案）。指标进不进预警、线在哪里，另有一份名单：`generate_html.py` 的 `WARN_ABS45` / `WARN_RANK`（见特例 10）。

### 3.2 美团门店级明细补数（`_read_mt_detail`）

`[评分数据\]有公式\全国评分数据*-美团（有公式）.xlsx` 的 `全国本期` / `全国上期` 是**门店 × 日期**明细，列号（0 基）：日期 4、门店名称 5、门店 id 6、复购率 16、消息回复率 18。

| 项 | 取值 |
| --- | --- |
| 门店名 | 第 5 列（F），形如 `喜茶(佛山顺德大良东乐路店)`，需 `_norm_store()` 去掉外壳并统一全/半角括号 |
| 补哪些指标 | 只补美团门店维度**缺列**的：`mt_reply`（8 月缺）、`mt_repeat`（9 月起缺） |
| 匹配顺序 | 规范化门店名 → 「战区框架表」（`门店名称` 第 15 列 → `美团外卖ID` 第 9 列）反查 ID → 明细第 6 列 |
| 取值 | 对门店**全部日期取平均** |
| 日期区间 | 顺带返回本期/上期的 `(起, 止)`，用作页面「数据周期 / 上一周期」文案（见 §3） |

- 为什么取全部日期平均：该文件自己的公式就是这么算的（`华南门店维度` 与 `全国本期` 同列均值一致，已核对 191/191 家；复购率也逐店核对过，一致到 4 位小数）；窗口不是「本周」。
- 当前两期：8 月 = `20260801–20260831`（上期 07-01–07-31）；9 月 = `20260901–20260913`（上期 08-01–08-31）。
- 只补美团门店维度里出现过的门店；督导/城市/区域层由门店聚合（`_agg` / `_wavg`）。

### 督导视图派生指标（`build_data.py` 的 `COMPOSITE`）

| 指标 | 公式 |
| --- | --- |
| `mt_quality` / `sg_quality` 商品质量分 | （满意度×30% + 包装满意度×10% + 复购率指标得分×20% + 食品安全负反馈率×20%）**÷ 0.8**（满分 5 分） |
| `mt_service` / `sg_service` 服务体验分 | （消息回复率×10% + 服务负反馈率×10%）**÷ 0.2**（满分 5 分） |
| 综合体验分 = 平台评分 `mt_score` / `sg_score` | 商品质量分×80% + 服务体验分×20%（满分 5 分） |

- 闪购的「满意度」取 `sg_taste_sat` 口味满意度；美团取 `mt_goods_sat`。
- **6 项底层指标权重合计 100%**：商品类 4 项合计 80%、服务类 2 项合计 20%。分类分必须**除以本维度总权重**（商品 ÷0.8、服务 ÷0.2）归一化回 5 分制；`COMPOSITE_DENOM` 存的就是这两个除数，**不要改回直接求和**。
- 两个分类分归一化后，`综合体验分 = 商品质量分×80% + 服务体验分×20%` 与「底层 6 项加权直接求和」数值等价，因此平台给的评分与分类分是同一 5 分制，可以横向比较（平台自身按未四舍五入的原始值计算，用看板两位小数复算会有 ±0.06 以内的尾差，属正常）。
- 由 `_apply_composites()` 在 `build_period()` 与 `merge()` 两处统一补齐，覆盖门店/督导/城市/区域四级；任一分项缺失则该派生指标为 `None`（显示 `—`）。
- 只调 `COMPOSITE` 时不需要重新解析 Excel，`python -X utf8 build_data.py --merge` 即可刷新 `data.json`。

**分类分的上期基数由上一期补**（`_fill_prev_from_previous()`，在 `merge()` 里）：

- 起因：源表在**门店 / 城市 / 区域**三级只给二级指标的「本期」一列，没有上期列，所以这三级一直算不出分类分环比（平台只在**督导**层给了「本期/上期/变化」三元组）。
- 规则：当 `上一期文件夹的 meta.period == 本期 meta.prevPeriod`（两期窗口首尾相接）时，用上一期的**本期值**填本期的 `prev`；**只填 `prev` 为空**的指标，平台自带的上期列不覆盖；上一期没有的门店/指标仍为 `None`（前端显示 `—`）。窗口对不上（半月期、缺上一期）就整段不补。
- 所以 `data.json` 里的 `prev` 有一部分**不是 Excel 里读出来的**，是跨期补的。平台两次给出的同窗口数值本身可能差 ≤0.03（门店集合变动），属已知现象，不要去"对齐"它。
- 城市层每个渠道各出一行、**城市名重复**，主键是 `(区域, 城市, _row_channel(row))`；别改成只用城市名，否则美团和闪购会互相串值。
- 闪购的「服务负反馈率」在督导表里是比率（0.88 这种），按特例 1 由门店聚合，所以闪购服务体验分的环比也依赖这条补数。

### 前端用到的指标分组

| 常量 | 内容 |
| --- | --- |
| `REGION_METRICS` | 区域总览 KPI 卡（7 张）：美团 = 总评分 + 商品质量分 + 服务体验分；闪购 = 总评分 + 商品质量分 + 服务体验分 + 商责取消率 |
| `BOTTOM_METRICS` | Bottom 5 表（9 项）：美团 = 总评分 + 商品质量分 + 服务体验分 + 消息回复率；闪购 = 总评分 + 商品质量分 + 服务体验分 + 消息回复率 + 商责取消率（比 KPI 卡多两个消息回复率百分比） |

- 区域总览的指标名走 `mLbl()`：拼上渠道前缀（`美团总评分` / `闪购商品质量分`），其中 `mt_score`/`sg_score` 显示为「总评分」，否则两张卡的「商品质量分」会重名。
- **门店全景排名（视图2）表格列序**：`#`（名次）→ 督导 → 城市 → 门店 → 勾选的指标列（第一个指标是评分时可展开 6 列二级指标）；每个单元格都是「数值 + 环比小字」——**包括展开的二级指标列**，两者都走 `dtext()` + `isWarn()`；改列序时 `storeDetailRows()` 的 `<th>` 与 `<td>` 两处要同步改。
- 所有表格用 `table.tbl{table-layout:fixed}` 让各列等宽（单元格 `overflow-wrap:anywhere` 兜底换行）；给某一列单独设宽会破坏等宽，改列宽前先确认这是需求。
- **响应式断点在 `generate_html.py` 的 `@media`**：`≤1024px` 平板（筛选栏换行、KPI `auto-fit`、面板 `overflow-x:auto`、表格 `min-width:560px`）、`≤760px` 手机（Header/KPI/筛选栏改纵向堆叠、KPI 两列、下拉与输入框全宽、表格 `min-width:660px`）、`≤420px` 小屏（字号与表格 `min-width` 再收窄）。窄屏的宽表靠 `min-width` + 面板横滑解决，**不要为了适配手机去删列或单独改某列宽度**（会破坏等宽约定）。
| `V3_MT_ORDER` / `V3_SG_ORDER` | 督导视图（督导表现表 + 门店明细，两张表列一致）：美团 = 评分 + 商品质量分 + 服务体验分；闪购 = 评分 + 商品质量分 + 服务体验分 + 商责取消率 |
| `SUB_MT` / `SUB_SG` | 门店明细里点击展开的二级指标 |

---

## 4. 已知特例（刻意如此，勿"修复"）

1. **闪购督导表最后一组列（20–22）被跳过**：它是比率，不是 0–5 分，与门店级口径不一致（8 月 `督导`、9 月 `华南督导` 两期的第 20 列都是 0.83～0.89 这样的比率）。因此闪购督导的「服务负反馈率」改由旗下门店聚合。
2. **闪购督导表没有商责取消率** → 由该督导旗下门店聚合（`_agg`）。
3. **闪购 `华南汇总` 战区行没有商责取消率** → 按城市**门店数加权**平均补齐（`_wavg`）。
4. **闪购 8 月期整表无上期数据**（9 月起平台已给上期）→ 8 月期闪购环比是 `—`。环比 UI 必须保留。
5. **闪购的「消息回复率」有两个指标**：`sg_reply_score`（N 列「消息回复率（10%）」的 0–5 得分）和 `sg_reply`（P/Q 列「本期/上期消息回复率」百分比，门店维度第 15/16 列）。两者都不要互相顶替。闪购 `华南汇总` 的百分比在第 14/15 列。
6. **美团复购率 `mt_repeat`**：8 月门店维度取第 15/16 列「本期/上期复购率」；9 月起这两列被换成了消息回复率，复购率改由（有公式）明细第 16 列按门店取全期均值（已逐店核对，与 8 月门店维度同列口径一致）。`华南汇总` 的复购率一直在第 14/15 列，不受影响。
7. **门店类型在评分数据里没有** → 取不到就记 `'未知'`（前端不使用该字段，不要因此报错或丢门店）。
8. **只在评分数据里出现、基础数据底表找不到的门店要保留**，不回退、不丢弃。
9. `华南分类型` sheet 当前未使用。
10. **预警命中时只给该单元格上浅红底**，不做整行/整列/整卡染色。规则在 `generate_html.py` 的 `isWarn()` / `kpiState()`（一份表驱动，视图1 KPI 卡 + 视图2 明细 + 视图3 两张表共用）：
    - `WARN_ABS45`（绝对 4.5 线）：评分、商品质量分、服务体验分，以及 4 项满意度（商品/包装/口味）；
    - `WARN_RANK` + `WARN_RANK_PCT=0.10`（相对线）：复购率指标得分、食品安全负反馈率、消息回复率得分、服务负反馈率——这些是平台**按名次区间折算**的 0–5 分，同期中位数只有 3.8~4.4，绝对线会把 82%~97% 的门店染红（实测），故改为“低于同期同渠道战区分布的后 10% 分位”（命中率 10%~23%）；
    - `sg_cancel > 0.003`（沿用旧值）。
    分类分（商品质量分/服务体验分）按绝对 4.5 线：评分、商品质量分命中率 22%~36%，但**服务体验分命中 61%（美团）/ 92%~94%（闪购）**——因为底层两项服务指标本就偏低，这是真实结论（服务是闪购短板），不是 bug；若要更有区分度，把它们从 `WARN_ABS45` 移到 `WARN_RANK` 即可。
11. **美团 `mt_reply` 的口径**：见 3.2，按门店对（有公式）明细**全部日期取平均**，不是本周值；门店名对不上时用「战区框架表」的 `美团外卖ID` 兜底（8 月 6 家、9 月 6 家靠这一条补上，如 `柳州万象城bakelab+店`、`三亚大悦城悦享店`），且只补美团门店维度里出现过的门店。
12. **`_rating_files()` 跳过名字含「有公式」的文件**：有公式文件单独由 `_formula_file()` 定位，别把它并进主扫描，否则同一门店会被两份文件互相覆盖。
13. **不要为了「对齐列号」去改 `STORE_COLS['美团']` 的第 15 列**：门店维度第 15/16 列的含义**各期不同**（8 月复购率、9 月起消息回复率），由 `_store_spec()` 按第 4 行表头动态决定；写死会把两个指标串在一起。

---

## 5. 改代码的约定

- **只改视觉，不动数据与功能**——这是本项目反复出现的需求边界。改 CSS 时不要顺手改计算逻辑。
- **面向使用者的说明在 `看板使用说明.md`**：改了指标口径、视图交互或更新流程，要同步改它；`python -X utf8 build_guide_docx.py` 会按 documents 预设（compact_reference_guide + 品牌茶绿标题）重新导出 `看板使用说明.docx`，两个文件内容保持一致。
- 配色/字号等集中在 `generate_html.py` 的 `:root`，改主题只动这里：

  | 变量 | 值 | 用途 |
  | --- | --- | --- |
  | `--bg` | `#F9F8F6` | 暖白页面底 |
  | `--card` | `#FFFFFF` | 卡片/表格底 |
  | `--ink` | `#1A1A1A` | 主文字 |
  | `--sub` | `#6E6B66` | 辅助文字 |
  | `--line` | `#E8E6E1` | 分割线 |
  | `--accent` | `#45684A` | 品牌茶绿（仅 Tab / 当前导航 / 少量强调 / 正向变化） |
  | `--good` / `--bad` / `--neu` | `#5E7E5A` / `#BE554B` / `#A3A09A` | 改善 / 恶化 / 无变化 |
  | `--warn-bg` / `--th-bg` | `#F9EFEC` / `#F1F0EC` | 异常单元格 / 表头 |

- 字体：正文 `Noto Sans SC`，数字 `Inter`，品牌标题 `Noto Serif SC`（`--font-sans` / `--font-num`）。
- 设计原则：暖白 + 白 + 墨黑 + 低饱和茶绿；**不加渐变、插画、复杂阴影、装饰性图标**；表头不要用绿色。
- `generate_html.py` 是往内联 `<script>` 里塞 JS 的，改完**必须验证内联 JS 语法**：

```python
import io, re, subprocess
s = io.open('dashboard.html', encoding='utf-8').read()
js = max(re.findall(r'<script[^>]*>(.*?)</script>', s, re.S), key=len)
io.open('_chk.js', 'w', encoding='utf-8').write(js)
print(subprocess.run(['node', '--check', '_chk.js'], capture_output=True, text=True).returncode)  # 0 = OK
```

---

## 6. 上线流程

`push` 到 `main` → Netlify 自动构建并更新固定链接。

```bash
python -X utf8 build_data.py
python -X utf8 generate_html.py
copy /y dashboard.html "部署_Netlify\index.html"
git add -A && git commit -m "..." && git push origin main
```

**关键**：`.gitignore` 排除了 `dashboard.html`、`部署_Netlify/index.html`、`双周基础数据*/`、`*评分数据*/`、`*基础数据*/`、`*.xlsx`、`_data_cache/`，所以线上构建只认 `data.json`——**更新数据时必须把 `data.json` 一起提交**，否则 Netlify 重建出来的还是旧数据。

`推送更新.bat` 只 `git add data.json`，**不会**提交代码改动；改了代码请手动 `git add -A`。

其他部署方式见 `README.md`；腾讯云 COS 见 `腾讯云COS部署步骤.md`。

---

## 7. 临时文件

调试脚本用完请删掉。`_data_cache/` 是构建缓存（可删，删了下次全量重建）。`.gitignore` 已忽略 `_pw_*.js`、`_shot_*.png` 等临时产物，但别让它们留在仓库里。
